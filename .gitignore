# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env
.env.local
.env*.local
.next/
node_modules/

# artifacts
oura-foundation.zip
git

# HealthLog reference repo (local design-reference only — never commit)
/Healthlog git/
Healthlog git/

# ephemeral claude-flow session files
.claude-flow/

# graphify auto-sync — generated outputs, regenerated by the git hooks.
# The setup itself (.claude/hooks/*, .graphifyignore) IS tracked; these are not.
/graphify-out/
/OuraVault/
